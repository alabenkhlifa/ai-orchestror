{
  "name": "sdd-orchestrator-assets",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end browser tests for SDD Orchestrator (Playwright).",
  "scripts": {
    "test:e2e": "playwright install chromium && playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
